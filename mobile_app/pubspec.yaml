name: walkietalkie_mobile
description: WalkieTalkie mobile voice bridge client.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^7.0.0
  cryptography: ^2.7.0
  qr_flutter: ^4.1.0
  web_socket_channel: ^3.0.1
  record: ^6.0.0
  flutter_tts: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
